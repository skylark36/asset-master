{
  "name": "asset-master-backend",
  "version": "1.0.0",
  "description": "Premium REST API & Yahoo Finance Caching Proxy for AssetMaster",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.1"
  }
}
